# Example environment variables for the frontend (Vite)
# Copy to frontend/.env and edit as needed.

# The base URL (including /api) for the backend API. Example:
# VITE_API_BASE="http://localhost:8000/api"
VITE_API_BASE="http://localhost:8001/api"

# If your backend runs on a different port (for example 8001), update this value:
# VITE_API_BASE="http://localhost:8001/api"
